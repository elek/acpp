# Demo project

A throwaway working directory used by the ACPP Android end-to-end test. The
test starts a session here and sends a prompt; the `rai acp fake` agent replies
with random text, so nothing in this directory actually matters — it just needs
to exist on the machine running the acpp server.
